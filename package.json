{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "watch:stylus": "stylus  -w **/**/*.styl",
    "push": "git add . && git commit -m 'up' && git push origin master",
    "deploy": "npm run build && cd dist && vercel --prod"
  },
  "license": "MIT",
  "devDependencies": {
    "vite": "^2.8.6",
    "vite-plugin-solid": "^2.2.6"
  },
  "dependencies": {
    "nib": "^1.1.2",
    "redux": "^4.2.0",
    "solid-js": "^1.3.13"
  }
}
